import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: {
          extralight: '#eee7e5',
          light: '#d5c5bf',
          DEFAULT: '#561902',
        },
      },
    }
  }
})